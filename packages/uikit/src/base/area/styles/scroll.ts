import { css } from "goober";

export const vScroll = css({
    overflowY: "auto",
    gridTemplateRows: "max-content",
});

export const hScroll = css({
    overflowX: "auto",
    gridTemplateColumns: "auto",
});
